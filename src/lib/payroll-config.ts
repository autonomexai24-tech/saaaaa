export const COMPANY_FIXED_SHIFT: { start: string; end: string; totalHours: number } = {
  start: "09:00",
  end: "18:00",
  totalHours: 8,
};

export const GRACE_PERIOD_MINUTES = 10;

export const OT_MULTIPLIER = 1.0;

export const PENALTY_PER_MINUTE = 5; // ₹5 per minute late after grace

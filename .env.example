# Salary Tracker — Environment Variables
# Copy this file to `.env` and fill in your actual values

# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:password@localhost:5432/salary_tracker

# Express API server port (do not change unless vite.config.ts proxy is also updated)
PORT=3001
